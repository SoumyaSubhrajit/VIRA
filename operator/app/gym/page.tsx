import GymOS from './GymOS';

export const metadata = {
  title: 'Gym OS | VIRA',
  description: 'Training, recovery and physique progress tracking for VIRA.',
};

export default function GymPage() {
  return <GymOS />;
}
